<?xml version="1.0" encoding="utf-8"?>
<!--
  widget_btn_state_light.xml — the same two states for light themes.

  Black overlays rather than white, matching how the light themes define
  surfaces.* — see ThemeLightDefault / ThemePaper / ThemeSepia. Slightly
  stronger than the dark pair because a dark overlay on a pale surface reads
  weaker at the same alpha.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android"
    android:enterFadeDuration="130"
    android:exitFadeDuration="180">

    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#26000000" />
            <corners android:radius="14dp" />
        </shape>
    </item>

    <item>
        <shape android:shape="rectangle">
            <solid android:color="#0f000000" />
            <corners android:radius="14dp" />
        </shape>
    </item>
</selector>
